//Including the header file
#include<stdio.h>
  int main()
  {
  	//Declaring the variable
  	int a,b,res;
  	
  	//Addition of the variable
  	 a=10;
  	 b=30;
  	 res=a+b;
  	 
  	 //print the result
  	 printf("\nnThe addition is %d",res);
  	 
  	 
  	 //Sustraction of the variable
  	  a=40;
  	  b=56;
  	  res=a-b;
  	  printf("\nnThe substraction of the variable is %d",res);
  	  
  	  //Dividation of the variable
  	  a=56;
  	  b=40;
  	  res=a/b;
  	  printf("\nThe dividation of the variable is %d",res);
		
	 //Multiplication of the variable
	 a=45;
	 b=67;
	 res=a*b;
	 printf("\nThe multiplication of the variable is %d",res); 
	 
	 //Modules of the varriable
	 a=60;
	 b=36;
	 res=a%b;
	 printf("\nThe modulus of the number is %d",res);}